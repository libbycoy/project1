<%= form_for (@user, :html=> { :multipart => true }) do |f| %>
  <p>
  <%= f.label "User Name:" %>
  <%= f.text_field :name %>

  <%= f.label "User image:" %>
  <%= f.cl_image_upload :image %>

  <%= f.submit "Create user" %>

<% end %>
